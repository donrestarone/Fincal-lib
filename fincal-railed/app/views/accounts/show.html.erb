<h1><%= @account.name %></h1>
<div>
	<%= link_to 'Back to Accounts', accounts_path(@accounts) %> 
</div>
<%= render 'single_account', an_account: @account %> 

<div>
	<%= link_to 'Edit', edit_account_path %> 
	<%= link_to 'Delete Account', account_path(@account), method: :delete, data: {confirm: "are you sure you want to delete this '#{@account.category}' account?"} %>
</div>