<%= link_to 'back to accounts', users_accounts_path %> 
<h1> Calculate Time Value For Your Accounts</h1>
<%= form_for :user_choice, url: tvom_choice_path  do |f|%>
<%= f.collection_select(:account,@accounts.all, :id, :name ) %>
 <%= f.select :choice, [['Calculate future value', 1],['Calculate present value', 2],['calculate future value annuity',3],['Calculate present value annuity', 4]] %>
 <%= f.submit 'Calculate' %>
<% end %> 


